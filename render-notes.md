# Render deployment source

This branch contains the complete deployable source for the CampusSafe Twin prototype. The static frontend uses hash routing so direct navigation works on static hosting without rewrite rules.
